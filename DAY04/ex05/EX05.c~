#include <stdlib.h>
#include <stdio.h>

int ft_sqrt(int nb)
{
  int res  = 0;
  int mod = (nb
  
}

   
int main()
{
  int index = 34;
  int res = 0;
  if(index >= 0 || index <= 2147483647)
    {
      res =(ft_sqrt(index));
      printf("%d ", res);
      printf("\n");
      return res;
    }
  
  else
    {
      return 0; //return ERREUR
    }
  
}
